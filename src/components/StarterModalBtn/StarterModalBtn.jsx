import React from "react";

const StarterModalBtn = (props)=>{
    return(
        <div>
            <button onClick={props.onClick}>Start game</button>
        </div>
    )
}

export default StarterModalBtn;
